# 記号・表記の統一ルール（全章共通）

全15回を通じて以下の記号を用いる。章をまたいで記号がぶれないよう、
新しい記号を導入する場合は必ず本表に追記すること。

> **教科書準拠**: 記号・用語は原則として
> 加藤・田岡『電力システム工学の基礎』に合わせている。
> 国際文献で別記号が使われるものは併記した。
> 用語の対応は [`教科書対応.md`](教科書対応.md) を参照。

## 1. 基本的な表記規約

| 規約 | 例 | 意味 |
|:---|:---|:---|
| イタリック小文字 | $v(t)$, $i(t)$ | 時間関数（瞬時値） |
| 大文字 | $V$, $I$ | 実効値（RMS） |
| ドット付き大文字 | $\dot{V}$, $\dot{I}$ | フェーザ（複素数） |
| 太字 | $\mathbf{Y}$, $\mathbf{J}$ | 行列 |
| 太字小文字 | $\mathbf{x}$, $\mathbf{b}$ | ベクトル |
| 上付き $^*$ | $\dot{I}^*$ | 複素共役 |
| 添字 $i, j, k$ | $V_i$ | 母線（バス）番号 |
| 上付き p.u. なし | $V_i = 1.02$ | 特記なき数値はパーユニット値 |

> **注意**: 本講義では**電圧・電流・電力は原則としてパーユニット（p.u.）で扱う**。
> 実単位を用いる箇所では \[kV\], \[MW\] 等の単位を明示する。

## 2. 電気量・電力（第1〜3回）

| 記号 | 意味 | 単位 |
|:---|:---|:---|
| $\dot{S}$ | 複素電力 $\dot{S} = P + jQ$ | VA, p.u. |
| $P$ | 有効電力 | W, p.u. |
| $Q$ | 無効電力 | var, p.u. |
| $\cos\phi$ | 力率 | — |
| $\dot{V}_i = V_i \angle \theta_i$ | 母線 $i$ の電圧フェーザ | V, p.u. |
| $V_i$ | 電圧の大きさ | V, p.u. |
| $\theta_i$ | 電圧位相角 | rad |
| $\theta_{ij}$ | 位相角差 $\theta_i - \theta_j$ | rad |
| $\dot{Z} = R + jX$ | インピーダンス | Ω, p.u. |
| $\dot{Y} = G + jB$ | アドミタンス | S, p.u. |
| $S_\text{base}$ | 基準容量 | MVA |
| $V_\text{base}$ | 基準電圧（線間） | kV |
| $Z_\text{base}$ | 基準インピーダンス $= V_\text{base}^2 / S_\text{base}$ | Ω |

## 3. 潮流計算（第4〜6回）

| 記号 | 意味 |
|:---|:---|
| $\mathbf{Y}_\text{bus}$ | **ノードアドミタンス行列**（教科書 4.1 節） |
| $Y_{ii}$ | **自己アドミタンス**（駆動点アドミタンス） |
| $Y_{ij} = G_{ij} + jB_{ij}$ | **相互アドミタンス**（伝達アドミタンス）、$i \neq j$ |
| $n$ | 母線総数 |
| $N_\text{PQ}$, $N_\text{PV}$ | PQ 母線数・PV 母線数 |
| $P_i^\text{sch}$, $Q_i^\text{sch}$ | 母線 $i$ の指定（scheduled）注入電力 |
| $P_i^\text{calc}$, $Q_i^\text{calc}$ | 現在の推定値から計算した注入電力 |
| $\Delta P_i$, $\Delta Q_i$ | ミスマッチ $= P_i^\text{sch} - P_i^\text{calc}$ |
| $\mathbf{J}$ | ヤコビアン行列 |
| $\mathbf{J} = \begin{bmatrix} \mathbf{H} & \mathbf{N} \\ \mathbf{M} & \mathbf{L}\end{bmatrix}$ | 4分割表記（$\mathbf{H}=\partial P/\partial\theta$ 等） |
| $\nu$ | 反復回数（上付き $^{(\nu)}$ で表す） |
| $\varepsilon$ | 収束判定閾値（既定 $10^{-6}$ p.u.） |

> **ノード種別の統一名称**（教科書 4.2 節）: **スラックノード**、**PV 指定ノード**（発電機ノード）、
> **PQ 指定ノード**（負荷ノード）、**浮遊ノード**（$P=Q=0$ の中継点）。
> 「ノード」は「母線」とも呼ぶ。本講義ではスラックノードを必ず**ノード番号 1** に取る。

## 4. 系統安定度（第7回）

| 記号 | 意味 | 単位 |
|:---|:---|:---|
| $\delta$ | 発電機の**相差角**（教科書 5.2 節） | rad |
| $\omega$ | ロータ角速度 | rad/s |
| $\omega_0 = 2\pi f_0$ | 定格角速度 | rad/s |
| $H$ | 慣性定数（国際標準）。本資料の主表記 | s |
| $M = \omega J$ | 慣性定数（**教科書 (5.4) 式**）。$M \simeq 2H/\omega_0$ | — |
| $P_a = P_m - P_e$ | **加速電力**（教科書 (5.5) 式） | p.u. |
| $K_s$ | **同期化力** $=\dfrac{VE'}{X}\cos\delta$（教科書は $S$ と表記） | p.u./rad |
| $S_A$ / $S_B$ | **加速エネルギー / 減速エネルギー**（教科書 5.3 節） | p.u.·rad |
| $D$ | 制動係数 | p.u./(rad/s) |
| $P_m$ | 機械入力 | p.u. |
| $P_e$ | 電気出力 | p.u. |
| $P_\text{max}$ | 電力動揺方程式の最大送電電力 | p.u. |
| $E'$ | 過渡リアクタンス背後電圧 | p.u. |
| $X_d'$ | 直軸過渡リアクタンス | p.u. |
| $\delta_\text{cr}$ | 臨界故障除去角 | rad |
| $X_d, X_d', X_d''$ | 同期／過渡／初期過渡リアクタンス | p.u. |
| $E, E', E''$ | 背後電圧／過渡背後電圧／初期過渡背後電圧 | p.u. |
| $t_\text{cr}$ | 臨界故障除去時間 | s |

**動揺方程式（本講義の標準形）**:
$$\frac{2H}{\omega_0}\frac{d^2\delta}{dt^2} = P_m - P_e - D\frac{d\delta}{dt}$$

## 5. 周波数制御・受給バランス（第8回）

教科書 第7章に準拠し、周波数は**大文字 $F$** で表す。

| 記号 | 意味 | 単位 |
|:---|:---|:---|
| $F_N$ | **定格周波数**（沖縄・西日本は 60 Hz）。第1〜7回の $f_0$ と同一 | Hz |
| $F_0$ | **無負荷時**の回転速度に相当する周波数 | Hz |
| $\Delta F$ | 周波数偏差 | Hz |
| $\varepsilon$ | **速度調定率** $=\dfrac{F_0-F_N}{F_N}\times100$（一般に 4〜5%） | % |
| $K_G$ | **発電力特性**（発電機の周波数特性） | MW/Hz |
| $\%K_G$ | $=\dfrac{100\times100}{\varepsilon F_N}$ | %MW/Hz |
| $K_L$ | 負荷の周波数特性（**自己制御性**） | MW/Hz |
| $K = K_G+K_L$ | **系統定数**。$\Delta P = K\Delta F$ | MW/Hz |
| $\%K$ | $=\dfrac{100K}{\text{系統容量}}$。実系統で 10〜20 %MW/Hz | %MW/Hz |
| $\Delta P$ | 需給アンバランス（**発電力が多い場合を正**） | MW |
| $\Delta P_T$ | 連系線潮流の変化（**A 系統から B 系統を正**） | MW |
| $AR$ | **地域要求量**（Area Requirement） | MW |
| $ACE$ | **地域制御偏差**（Area Control Error） | MW |
| $H_\text{sys}$ | 系統慣性定数（教科書外。第7回から継続） | s |
| RoCoF | 周波数変化率 $dF/dt$（教科書外） | Hz/s |

> **単位の注意（最頻出の誤り）**: 実務では **%MW/0.1Hz** が使われ、
> これは **%MW/Hz の 1/10** の値である。
> 例: 15 %MW/Hz = 1.5 %MW/0.1Hz。教科書の例題・問題はすべて %MW/0.1Hz で与えられる。

> **略語の統一**: LFC（負荷周波数制御）、**ELD（経済負荷配分運転）**、
> FFC（定周波数制御）、TBC（周波数バイアス連系線潮流制御）。
> ガバナによる制御は**調速制御（ガバナ制御）**と呼ぶ。
> ※ 教科書は EDC / AGC の語を用いないため、本資料も **ELD** に統一する。

## 6. インバーター・系統連系（第9回）

| 記号 | 意味 | 単位 |
|:---|:---|:---|
| $V_\text{dc}$ | 直流リンク電圧 | V |
| $m_a$ | 振幅変調率 (amplitude modulation ratio) | — |
| $m_f$ | 周波数変調率 (frequency modulation ratio) | — |
| $f_\text{sw}$ | スイッチング周波数 | Hz |
| $f_1$ | 基本波周波数 | Hz |
| $\text{THD}$ | 全高調波歪率 | % |
| $L_f$, $C_f$ | 連系フィルタのインダクタンス・キャパシタンス | H, F |
| $v_d, v_q$ / $i_d, i_q$ | dq 変換後の電圧・電流 | p.u. |

## 7. 予測・機械学習（第10〜13回）

| 記号 | 意味 |
|:---|:---|
| $y_t$ | 時刻 $t$ の実測値（需要 \[MW\] または発電量） |
| $\hat{y}_t$ | 時刻 $t$ の予測値 |
| $e_t = y_t - \hat{y}_t$ | 予測誤差 |
| $\mathbf{x}_t$ | 時刻 $t$ の特徴量ベクトル |
| $h$ | 予測ホライズン（先何ステップか） |
| $T$ | 評価対象のサンプル数 |
| $G$ | 全天日射量 \[W/m²\] |
| $T_a$ | 気温 \[°C\] |
| $u_w$ | 風速 \[m/s\] |

**予測精度指標（本講義の定義を統一）**:

$$\text{MAE} = \frac{1}{T}\sum_{t=1}^{T}|y_t - \hat{y}_t|, \qquad
\text{RMSE} = \sqrt{\frac{1}{T}\sum_{t=1}^{T}(y_t - \hat{y}_t)^2}$$

$$\text{MAPE} = \frac{100}{T}\sum_{t=1}^{T}\left|\frac{y_t - \hat{y}_t}{y_t}\right| \ [\%]$$

> **注意**: MAPE は $y_t \approx 0$ で発散するため、太陽光発電量の評価には
> **定格容量で正規化した nRMSE** を併用する（第10, 12回で明示）。

## 8. 最適化（第14回）

| 記号 | 意味 |
|:---|:---|
| $\mathbf{u}$ | 制御変数ベクトル（発電機出力・電圧設定値など） |
| $\mathbf{x}$ | 状態変数ベクトル |
| $C_i(P_{Gi})$ | 発電機 $i$ の燃料費関数 \[¥/h\] |
| $a_i, b_i, c_i$ | 燃料費関数 $C_i = a_i P_{Gi}^2 + b_i P_{Gi} + c_i$ の係数 |
| $\lambda$ | ラグランジュ乗数（等価燃料費・限界費用） |
| $\mu$ | 不等式制約に対する乗数 |
| $P_{Gi}^\text{min}, P_{Gi}^\text{max}$ | 発電機出力の下限・上限 |

## 9. ギリシャ文字の使い分け（衝突回避）

複数の意味を持ちうる文字について、本講義での用法を固定する。

| 文字 | 本講義での用法 | 使わない用法 |
|:---|:---|:---|
| $\delta$ | 発電機ロータ角のみ | 微小変化（→ $\Delta$ を使う） |
| $\theta$ | 母線電圧位相角のみ | 力率角（→ $\phi$ を使う） |
| $\phi$ | 力率角 | 磁束（→ $\Phi$ を使う） |
| $\lambda$ | ラグランジュ乗数 | 波長（本講義では登場しない） |
| $\omega$ | 角速度・角周波数 | — |
| $\eta$ | 効率 | 学習率（→ $\alpha$ を使う） |
| $\alpha$ | 学習率（機械学習） | 点弧角（→ 本講義では登場しない） |
